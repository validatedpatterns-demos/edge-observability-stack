apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "grafana.fullname" . }}-config
data:
  datasources.yaml: |
    {{ toYaml .Values.datasources | nindent 4 }}

  providers.yaml: |
    {{ toYaml .Values.providers }}
